set all routes manually

paginate search results

paginate movies

cancancan for admin/users

add age to users, check ID for adult search

create an "on-deck" controller and views
  load movies into "on-deck" which will
  be the working list from which the winning
  movie will be chosen

functionality: move to list
functionality: copy to list

don't allow duplicate movies on any list

make a layout/_list_movies.html.erb

put search form in search results (navbar?)

import from Netflix `My List`
